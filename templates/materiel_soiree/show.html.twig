{% extends 'base.html.twig' %}

{% block title %}MaterielSoiree{% endblock %}

{% block body %}
    <h1>MaterielSoiree</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel_soiree.id }}</td>
            </tr>
            <tr>
                <th>DateReservation</th>
                <td>{{ materiel_soiree.dateReservation ? materiel_soiree.dateReservation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_materiel_soiree_index') }}">back to list</a>

    <a href="{{ path('app_materiel_soiree_edit', {'id': materiel_soiree.id}) }}">edit</a>

    {{ include('materiel_soiree/_delete_form.html.twig') }}
{% endblock %}
